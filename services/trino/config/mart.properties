# Trino DuckDB mart catalog — for local dev / CI testing only
# NOT for production deployment (use Iceberg REST catalog instead)
#
# Mirrors the local docker-compose setup in sector2-core/superset/

connector.name=hive
hive.metastore=file
hive.metastore.catalog.dir=file:///data/hive-metastore
hive.non-managed-table-writes-enabled=true
fs.hadoop.enabled=true